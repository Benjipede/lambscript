true = \x y ~ x;
false = \x y ~ y;
and = \p q ~ p q;
or = \p q ~ q p;
not =  \p a b ~ a b p;
tuple = \a b p ~ b a p;
fst = \p ~ true p;
snd = \p ~ false p;
curry = \f a b ~ b a tuple $$  f;
uncurry = \f p ~ p snd $ p fst $ f;
flip = \x y f ~ x y f;
id = \x ~ x;
